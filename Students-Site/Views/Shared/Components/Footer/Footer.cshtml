@model Students_Site.Models.Home.IndexModel

<footer class="border-top footer text-muted">
	<div class="container">
		<div>
			Кол-во студентов: @Model.StudentCount
			Кол-во преподавателей: @Model.TeacherCount
			Текущие время и дата: <span id="currentDateTime"></span>
		</div>
	</div>
</footer>

<script>
	function startTime() {
		var today = new Date();
		var date = today.getFullYear() + '-' + (today.getMonth() + 1) + '-' + today.getDate();
		var time = today.getHours() + ":" + checkTime(today.getMinutes()) + ":" + checkTime(today.getSeconds());
		var dateTime = date + ' ' + time;

		var spanCurrent = document.getElementById("currentDateTime");
		spanCurrent.textContent = dateTime;

		setTimeout(startTime, 500);
	}

	function checkTime(i) {
		if (i < 10) { i = "0" + i };  // add zero in front of numbers < 10
		return i;
	}

	startTime();
</script>